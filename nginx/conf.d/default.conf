server {
    listen 80;
    server_name localhost;  # 生产环境替换为你的域名，如 api.starlink.tech

    # ===== 定制首页 =====
    location = / {
        root /usr/share/nginx/html;
        index index.html;
        try_files /index.html =404;
    }

    # ===== 定制首页静态资源 =====
    location ~* \.(html|css|js|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        root /usr/share/nginx/html;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # ===== New API 管理后台 =====
    location /admin {
        proxy_pass http://starlink-api:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
    }

    # ===== API 接口（/v1/chat/completions 等） =====
    location /v1 {
        proxy_pass http://starlink-api:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_buffering off;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 600s;
        client_max_body_size 64m;
    }

    # ===== New API 内部 API =====
    location /api {
        proxy_pass http://starlink-api:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
    }

    # ===== New API 静态资源 =====
    location /static {
        proxy_pass http://starlink-api:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        expires 30d;
    }

    # ===== 兜底：其他路径转发给 New API =====
    location / {
        proxy_pass http://starlink-api:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
    }
}

# ===== HTTPS 配置（生产环境取消注释并配置证书） =====
# server {
#     listen 443 ssl http2;
#     server_name api.starlink.tech;
# 
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
# 
#     # 其他 location 配置同上
#     location / { proxy_pass http://starlink-api:3000; ... }
# }
# 
# server {
#     listen 80;
#     server_name api.starlink.tech;
#     return 301 https://$host$request_uri;
# }
